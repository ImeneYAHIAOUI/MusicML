/**
 * Generic message interface.
 * @category MIDI Messages
 */
export interface MidiMessage {
    channel: number;
    command: number;
    data1: number;
    data2: number;
}
type MidiData = Uint8Array;
export declare const isChannelMode: (channel: number) => boolean;
export declare const makeMessage: ({ channel, command, data1, data2, }: MidiMessage) => Uint8Array;
export declare const toMidiMessage: ([status, data1, data2,]: MidiData) => MidiMessage;
export {};
