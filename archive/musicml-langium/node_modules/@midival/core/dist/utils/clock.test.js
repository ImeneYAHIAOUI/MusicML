"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const clock_1 = require("./clock");
describe("quarternoteToBPM", () => {
    it("should properly compute BPM = 60", () => {
        expect((0, clock_1.quarternoteToBPM)(1000 / 24)).toEqual(60);
    });
    it("should properly compute BPM = 100", () => {
        expect((0, clock_1.quarternoteToBPM)(600 / 24)).toEqual(100);
    });
});
describe("ticksToBPM", () => {
    it("should properly compute BPM = 60 (every second)", () => {
        expect((0, clock_1.ticksToBPM)([1000, 1041.6666666667, 1000 + 2 * 41.6666666667])).toBeCloseTo(60);
    });
    it("should properly compute BPM = 100 (every 600ms)", () => {
        expect((0, clock_1.ticksToBPM)([600, 625, 650, 675, 700])).toBeCloseTo(100);
    });
    it("should properly return 0 when array is empty", () => {
        expect((0, clock_1.ticksToBPM)([])).toBeCloseTo(0);
    });
});
