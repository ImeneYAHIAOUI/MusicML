import { UnregisterCallback } from "@hypersphere/omnibus";
import { IMIDIInput, OnMessageCallback } from "./IMIDIInput";
export interface MidiDeviceProps {
    id: string;
    name: string;
    manufacturer: string;
}
export declare class MockMIDIInput implements IMIDIInput {
    private _name;
    private _id;
    private _manufacturer;
    private _callback;
    constructor({ id, name, manufacturer }: MidiDeviceProps);
    onMessage(callback: OnMessageCallback): Promise<UnregisterCallback>;
    sendMessage(data: Uint8Array): void;
    get name(): string;
    get id(): string;
    get manufacturer(): string;
}
